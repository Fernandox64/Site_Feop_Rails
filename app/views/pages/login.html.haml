!!!
%html
%head
  %Title Feop
  = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true 
  = javascript_include_tag 'application', 'data-turbolinks-track' => true 
  %link{:rel => "stylesheet", :href => "http://cdnjs.cloudflare.com/ajax/libs/normalize/3.0.1/normalize.mim.css"}
  %link{:rel => "stylesheet", :href => "http://maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.mim.css"}
  = csrf_meta_tags

%body
  %header
    .wrapper.clearfix
      %nav 
        - if user_signed_in?
          = link_to current_user.name, edit_user_registration_path
          = link_to "Adicionar Nova Notícia", new_post_path, class: "button"
          = link_to "Adicionar Novo Projeto a Galeria", new_gallery_path, class: "button"
          = link_to t('.new', :default => t("helpers.links.Novo Edital")),new_edital_path, class: "button"
        - else 
          = link_to "Log In", new_user_session_path, class: "button"
          //= link_to "Registrar novo Usuario", new_user_registration_path, class: "button"
    
  
  %p.notice= notice
  %p.alert= alert
